@page "/Login"
@model Booking.WebUI.Pages.Account.LoginModel

<section class="identity">
    <form id="account" method="post">
        <h2 class="text-center">Zaloguj się</h2>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-floating">
            <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" />
            <label asp-for="Input.Email" class="form-label"></label>
            <span asp-validation-for="Input.Email" class="text-danger"></span>
        </div>
        <div class="form-floating">
            <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" />
            <label asp-for="Input.Password" class="form-label"></label>
            <span asp-validation-for="Input.Password" class="text-danger"></span>
        </div>
        <div>
            <div class="checkbox">
                <label asp-for="Input.RememberMe" class="form-label">
                    <input class="form-check-input" asp-for="Input.RememberMe" />
                    @Html.DisplayNameFor(m => m.Input.RememberMe)
                </label>
            </div>
        </div>
        <div>
            <p>
                <a id="forgot-password" asp-page="./ForgotPassword" class="link-dark">Zapomniałem hasła</a>
            </p>
        </div>
        <div>
            <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-dark mb-2">Zaloguj</button>
        </div>
    </form>
    @*<div>
        @if ((Model.ExternalLogins?.Count ?? 0) != 0)
        {
            <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
                <div>
                    <p>
                        @foreach (var provider in Model.ExternalLogins)
                        {
                            if (provider.DisplayName == "Google")
                            {
                                <button type="submit" class="w-100 btn btn-lg btn-primary" name="provider" value="@provider.Name">
                                    <i class="fa fa-google">Zaloguj się przy użyciu @provider.DisplayName</i>
                                </button>
                            }
                            else
                            {
                                <button type="submit" class="w-100 btn btn-lg btn-dark" name="provider" value="@provider.Name">
                                    Zaloguj się przy użyciu @provider.DisplayName
                                </button>
                            }
                        }
                    </p>
                </div>
            </form>
        }
    </div>*@
    <div>
        <a asp-page="/Account/Register" class="w-100 btn btn-lg btn-dark">Przejdź do rejestracji</a>
    </div>
</section>

@section Scripts {
<partial name="_ValidationPartial" />
<script defer src="https://use.fontawesome.com/releases/v5.0.8/js/brands.js" integrity="sha384-sCI3dTBIJuqT6AwL++zH7qL8ZdKaHpxU43dDt9SyOzimtQ9eyRhkG3B7KMl6AO19" crossorigin="anonymous"></script>

}
